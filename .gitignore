# Environment and secrets
.env
settings.env
secrets.json
*.log

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/
.eggs/
dist/
build/
venv/
.venv/
.python-version

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Project
registry/
.cursor/debug-*.log
.healthai_setup_complete

# Локальная SQLite (создаётся при первом запуске)
database/*.db

# Bootstrap / отчёты / сгенерированные артефакты (не в истории git)
get-pip.py
startup_report.json
export_import/*.pdf
analytics/*.png

# Локальные веса ML в assets (при необходимости положить свои — не коммитить бинарники)
assets/models/*.onnx
assets/models/*.pkl
assets/models/*.h5

# Веса vision в репозитории не обязательны (большой файл)
models/vision/*.pth

# Локальные веса Ollama (скачиваются при первом запуске, см. scripts/ensure_ollama_models.*)
models/ollama/
models/ollama-serve.log
# Старый каталог sentence-transformers больше не используется; не коммитить случайные снимки
models/nlp/
